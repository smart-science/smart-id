{
    "name": "@smart-science/sid",
    "version": "0.2.0",
    "description": "SID: generator, parser, formatter, and checksum verifier",
    "keywords": [
        "sid",
        "smart-id",
        "smartid",
        "crockford",
        "checksum",
        "generator",
        "parser",
        "formatter",
        "verifier"
    ],
    "author": "Martin Winkler",
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/smart-science/smart-id.git"
    },
    "bugs": {
        "url": "https://github.com/smart-science/smart-id/issues"
    },
    "homepage": "https://github.com/smart-science/smart-id#readme",
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "src",
        "CHANGELOG.md"
    ],
    "exports": {
        ".": {
            "bun": "./src/index.ts",
            "types": "./dist/index.d.ts",
            "default": "./dist/index.js"
        }
    },
    "sideEffects": false,
    "engines": {
        "node": "^20.19.0 || >=22.12.0",
        "bun": ">=1.3.0"
    },
    "publishConfig": {
        "access": "public"
    },
    "scripts": {
        "clean": "rm -rf dist",
        "build": "bun run clean && bun run build:types && bun run build:js",
        "build:js": "bun build ./src/index.ts --outdir ./dist --target node --format esm",
        "build:types": "tsc --emitDeclarationOnly",
        "test": "bun test && bun run test:types",
        "test:types": "tsc --noEmit -p tsconfig.test.json",
        "test:dist": "node tests/smoke/smoke.mjs && node tests/smoke/smoke.cjs",
        "test:package": "mkdir -p node_modules/.cache/sid-pack && bun pm pack --filename node_modules/.cache/sid-pack/package.tgz --quiet && attw --profile esm-only node_modules/.cache/sid-pack/package.tgz",
        "coverage": "bun test --coverage",
        "lint": "biome check .",
        "lint:fix": "biome check --write .",
        "check": "bun run lint && bun run test && bun run build && bun run test:dist && bun run test:package",
        "prepublishOnly": "bun run check"
    },
    "devDependencies": {
        "@arethetypeswrong/cli": "^0.18.5",
        "@biomejs/biome": "^2.5.11",
        "@types/bun": "^1.4.0",
        "fast-check": "^4.10.1",
        "typescript": "^5.9.3"
    }
}
